"""
[DEPRECATED] 통합 객체 모델 - Vector DB 단독 모드로 전환됨

이 파일은 더 이상 사용되지 않습니다.
모든 데이터는 Vector DB(Qdrant)에 직접 저장됩니다.
"""

# 파일 내용 제거됨 - Vector DB 단독 모드로 전환
# 기존 IntegratedObject 모델은 더 이상 필요하지 않습니다.

# 하위 호환성을 위해 빈 클래스 제공 (기존 import 에러 방지)
class IntegratedObject:
    """[DEPRECATED] 더 이상 사용되지 않는 통합 객체 모델"""
    pass