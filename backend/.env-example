# Freshdesk Custom App Backend - 환경변수 템플릿
# 이 파일을 복사하여 .env 파일을 만들고 실제 값으로 수정하세요

# =================================================================
# AWS 설정 (운영환경용 - 민감정보는 AWS Secrets Manager 사용)
# =================================================================
AWS_REGION=us-west-2
# AWS_ACCESS_KEY_ID=your-aws-access-key-id
# AWS_SECRET_ACCESS_KEY=your-aws-secret-access-key

# =================================================================
# 멀티테넌트 데이터베이스 설정
# =================================================================
# 운영환경: PostgreSQL (설정 시 PostgreSQL + 테넌트별 스키마 사용)
# DATABASE_URL=postgresql://username:password@host:port/database_name
# 개발환경: SQLite (DATABASE_URL 미설정시 자동으로 SQLite 사용)

# =================================================================
# 멀티플랫폼 표준 설정 (헤더 기반 API 우선, 환경변수는 fallback용)
# =================================================================
# 개발/테스트용 기본 회사 ID (운영 환경에서는 X-Company-ID 헤더 사용)
COMPANY_ID=your_company_id
API_KEY=your_api_key
# DOMAIN: 도메인 이름만 입력 (예: "your-company") 또는 전체 도메인 (예: "your-company.freshdesk.com")
# 시스템에서 자동으로 company_id를 추출하여 X-Company-ID 헤더에 사용합니다
DOMAIN=your_platform_domain
PLATFORM=freshdesk

# =================================================================
# Qdrant Cloud 벡터 데이터베이스 설정
# =================================================================
QDRANT_URL=https://your-qdrant-url
QDRANT_API_KEY=your_qdrant_api_key

# =================================================================
# LLM API 키 설정 (Multi-LLM Router용)
# =================================================================
ANTHROPIC_API_KEY=your_anthropic_api_key
OPENAI_API_KEY=your_openai_api_key
GOOGLE_API_KEY=your_google_api_key
# GEMINI_API_KEY는 더 이상 사용하지 않습니다. GOOGLE_API_KEY로 통일했습니다.

# =================================================================
# 애플리케이션 설정
# =================================================================
COMPANY_ID=your_company_id
PROCESS_ATTACHMENTS=true
EMBEDDING_MODEL=text-embedding-3-small
LOG_LEVEL=INFO
MAX_TOKENS=4096

# =================================================================
# 개발 환경 설정 (선택사항)
# =================================================================
DEBUG=false
HOST=0.0.0.0
PORT=8000

# =================================================================
# 보안 설정 (향후 확장)
# =================================================================
# SECRET_KEY=your-secret-key-for-jwt
# ALLOWED_ORIGINS=http://localhost:3000,https://your-domain.freshdesk.com

# =================================================================
# 캐시 및 성능 설정 (선택사항)
# =================================================================
# REDIS_URL=redis://localhost:6379
# CACHE_TTL=3600
# MAX_CONCURRENT_REQUESTS=10

# =================================================================
# MCP Server API Keys (선택사항)
# =================================================================
